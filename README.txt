  Este repositorio fue creado con el fin de almacenar los codigos independientes de cada funcion referente al proyecto con arduino de una máquina de garra en donde, se usarán sensores, motores, una placa de arduino, y por supuesto todo lo que compone la parte mecánica para el desarrollo

The current repository contains a group of C++ files that will work as different methods for creating a claw machiine using arduino.

for creating the machine you must have, a slide system that will work with 2 servoMotors, in my case, i used rails, another servomotor for the reel, and 3 micro-servomotors for the claw, also 4 infrared leds emisors, and 4 infrared receptors, this ones will be used for, checking if a coing get in the machine, checking if an object left the machine as a prize, define limits for the claw when it will drop the prize for his own.

a Joystick for controlling the movement of the claw, and a button for sending the signal to grab the object and end the interaction.

You will find the full code in a week, for now i'm working in it, so it work with only one file.
